server {
    listen 80;

    location /weekly-bloom {
        alias /usr/share/nginx/html/weekly-bloom;
        index index.html index.htm;
        try_files $uri $uri/ /weekly-bloom/index.html;
    }

    location = / {
        return 301 /weekly-bloom/;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public";
    }
}