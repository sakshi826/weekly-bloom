server {
    listen 80;

    location /app/weekly-bloom {
        alias /usr/share/nginx/html/app/weekly-bloom;
        index index.html index.htm;
        try_files $uri $uri/ /app/weekly-bloom/index.html;
    }

    location = / {
        return 301 /app/weekly-bloom/;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|otf)$ {
        root /usr/share/nginx/html;
        expires 1y;
        add_header Cache-Control "public";
    }
}